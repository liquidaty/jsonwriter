# Generated by ./configure
/config.mk
/config.mk.log

# Build output (the Makefile builds into the parent dir by default, but allow
# an in-tree build/ too)
/build/

# Remote-build protocol scratch files (see SYNC-PROTOCOL.md)
/changed-files.txt
/result.txt

# Dockerized-fuzzing findings (make df-run)
/tests/fuzz-findings/
